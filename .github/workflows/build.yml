name: build-and-push

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:

# La imagen vive en el registro del propio repo (ghcr.io), así que no hacen
# falta secrets: Actions inyecta GITHUB_TOKEN y basta con pedirle packages:write.
permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/yoelsilva/k3s-mapper-to-alloy

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Comprobar sintaxis
        run: python3 -m py_compile mapper.py

      - name: Validar manifiestos
        run: |
          python3 -m pip install --quiet pyyaml
          for f in deploy/*.yaml; do python3 -c "import sys,yaml; list(yaml.safe_load_all(open('$f')))" || exit 1; done
          python3 -m json.tool config.example.json > /dev/null

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Login en ghcr.io
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Etiquetas:
      #   push a main  -> :main y :sha-xxxxxxx
      #   tag v1.2.3   -> :1.2.3, :1.2 y :latest
      - name: Etiquetas de imagen
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=sha-
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Build y push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
